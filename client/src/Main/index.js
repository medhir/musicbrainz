import React, { Component } from 'react'
import Search from '../Search'

class Main extends Component {
    render () {
        return (
            <main>
                <Search />
            </main>
        )
    }
}

export default Main